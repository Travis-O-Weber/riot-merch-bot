# ===========================================
# RIOT MERCH BOT CONFIGURATION
# ===========================================
# Copy this file to .env and fill in your values

# ----- MODE SETTINGS -----
# DRY_RUN: 1 = navigate only, 0 = full automation
DRY_RUN=1

# CHECKOUT_ENABLED: 1 = proceed to checkout, 0 = stop after cart
CHECKOUT_ENABLED=0

# FULL_SEND: 1 = complete purchase, 0 = stop before final submit
FULL_SEND=0

# KEEP_OPEN: 1 = keep browser open after completion
KEEP_OPEN=1

# HEADLESS: 1 = run without visible browser
HEADLESS=0

# ----- RIOT ACCOUNT SETTINGS -----
# Multiple accounts supported - bot will iterate through each
# Format 1: Numbered accounts (RIOT_USER_1, RIOT_PASS_1, etc.)
RIOT_USER_1=
RIOT_PASS_1=

RIOT_USER_2=
RIOT_PASS_2=

RIOT_USER_3=
RIOT_PASS_3=

# Format 2: JSON array (alternative to numbered format)
# RIOT_ACCOUNTS=[{"username":"user1","password":"pass1"},{"username":"user2","password":"pass2"}]

# Maximum accounts to attempt (0 = no limit)
MAX_ACCOUNTS=0

# ----- PRODUCT SETTINGS -----
# Use pipe (|) to specify synonyms/aliases for fuzzy matching
# Example: PRODUCT1="VLRNT // FRGMT WNGMN Keychain Plush|WNGMN Keychain|Wingman Plush"
PRODUCT1="VLRNT // FRGMT WNGMN Keychain Plush"
QTY1=1

# Additional products (optional)
PRODUCT2=
QTY2=1

PRODUCT3=
QTY3=1

# ----- DISCOUNT CODE -----
# Leave empty to skip discount code application
DISCOUNT_CODE=

# ----- SHIPPING INFORMATION -----
# Leave any field empty to keep website defaults
EMAIL=
FIRST_NAME=
LAST_NAME=
PHONE=
ADDRESS1=
ADDRESS2=
CITY=
STATE=
ZIP=
COUNTRY=United States

# ----- PAYMENT INFORMATION -----
# Leave empty to skip payment (manual entry required)
CARD_NUMBER=
CARD_EXP_MONTH=
CARD_EXP_YEAR=
CARD_CVV=

# ----- BROWSER SETTINGS -----
# Connect to existing Chrome browser running with --remote-debugging-port
# Launch Chrome first with: chrome.exe --remote-debugging-port=9222 --user-data-dir="%USERPROFILE%\ChromeDebug"
CONNECT_EXISTING=0
# IMPORTANT: Use 127.0.0.1 instead of localhost to avoid IPv6 (::1) connection issues
CDP_ENDPOINT=http://127.0.0.1:9222

# Path to Brave browser (optional, falls back to Chromium when CONNECT_EXISTING=0)
BRAVE_PATH="C:\Program Files\BraveSoftware\Brave-Browser\Application\brave.exe"
USER_DATA_DIR=
PROFILE_DIR=Default

# Multi-account browser stability:
# 1 = Use fresh browser context for each account (more stable, recommended)
# 0 = Reuse same browser context (may crash if using persistent profile)
MULTI_ACCOUNT_FRESH_CONTEXT=1

# ----- TIMEOUT SETTINGS (in ms) -----
# These are auto-adjusted in FULL_SEND mode
NAV_TIMEOUT_MS=45000
ACTION_TIMEOUT_MS=30000
MAX_RETRIES=3

# ----- SEARCH SETTINGS -----
# Fuzzy match threshold (0.0 - 1.0, higher = stricter)
FUZZY_THRESHOLD=0.5
